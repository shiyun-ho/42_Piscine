int
     strncmp(const char *s1, const char *s2, size_t n);

DESCRIPTION
     1. Compares the null-terminated strings s1 and s2.
	 2. Compares not more than n characters
	 3. Characters that appear after a ‘\0’ character are not
     compared.

RETURN VALUES
     1. Returns an integer greater than, equal to, or less than 0,according as the string s1 is greater than, equal to, or less than the string s2.  The comparison
     is done using unsigned characters, so that ‘\200’ is greater than ‘\0’.
